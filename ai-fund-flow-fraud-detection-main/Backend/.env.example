MONGO_URI=mongodb+srv://<username>:<password>@cluster.mongodb.net/frauddb
PORT=5000
KAFKA_BROKERS=localhost:9092
# Optional: Set these if using managed Kafka like Upstash
KAFKA_USERNAME=
KAFKA_PASSWORD=
